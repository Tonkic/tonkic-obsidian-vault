经典用法：
```C++
// 一个无重复的数组，选定一个i位置 找到k位置 离i左边最近/右边最近且a[k]<(或大于)a[i]
// 建立一个栈，遍历数组，实现大压小的原则，如果栈中出现a压b的情况，那么数组中b ... a中间的数一定是>a的，所以b是a左边离a最近且<a 的数
// 当出现x，num[x]<num[a],不符合大压小，x将a释放，x是a右边离a最近且小于a的数的下标，因为x是遇到的第一个<a的数才能将a释放
```