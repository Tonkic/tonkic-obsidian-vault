```mermaid
%%{
  init: {
    'theme': 'base',
    'themeVariables': {
      'primaryColor': '#ECECFD',
      'primaryTextColor': '#222',
      'primaryBorderColor': '#999',
      'lineColor': '#666',
      'secondaryColor': '#F9F9F9',
      'tertiaryColor': '#FFF',
      'fontFamily': 'sans-serif'
    }
  }
}%%
graph TD
    subgraph " "
    direction TB
    style Start fill:#FFF,stroke:#FFF

        User[fa:fa-user 用户] --> Prompt["fa:fa-keyboard <b>Prompt (P)</b><br>'a photo of a...<br>Boeing 727-200'"]
        Prompt --> T2I1(fa:fa-magic T2I 生成 V1)
        T2I1 --> V1[fa:fa-image V1 图像<br>(错误的 A320)]
        V1 --> VLM_Diagnosis

    Start( ) -. "<b>[ 循环开始 ]</b>" .-> VLM_Diagnosis

    %% 1. VLM 诊断 (核心)
    subgraph "STEP 1: VLM 诊断 (utils.py)"
        direction TB
        VLM_Diagnosis{fa:fa-brain VLM 诊断}
        VLM_Diagnosis -- "P + V1" --> Check{fa:fa-question-circle VLM 检查...}
        Check -- " <b>status: 'error'</b> " --> VLM_JSON[fa:fa-file-code <b>VLM JSON 输出</b><br>status: 'error'<br>error_type: 'wrong_concept'<br>features: ['three engines', 'T-tail']]
        Check -- " <b>status: 'success'</b> " --> Success(fa:fa-check-circle <b>成功!</b>)
    end

    %% 2. 调度器 (核心)
    subgraph "STEP 2: Smart RAG 调度器 (dispatcher.py)"
        direction TB
        Dispatcher[fa:fa-route <b>智能调度器</b>]
        VLM_JSON --> Dispatcher
        Dispatcher -- "读取 'error_type' & 'features'" --> RAG_Flow
    end

    %% 3. RAG 流程 (核心)
    subgraph "STEP 3: RAG 流程 (retrieval.py)"
        direction TB
        RAG_Flow(<b>RAG 检索 (不作弊)</b>)
        RAG_Flow -- "Query: ['three engines', ...]" --> Stage1(fa:fa-search 阶段 1: 语义检索)
        Stage1 --> Candidates[fa:fa-images Top-K 候选]
        
        %% 记忆/排除列表
        Memory[fa:fa-memory 记忆<br>(排除列表)]
        Memory -. "过滤已失败的" .-> Filtered(fa:fa-filter 过滤后候选)
        Candidates --> Filtered

        Filtered --> Stage2(fa:fa-ranking-star 阶段 2: ZSC 重排)
        Stage2 -- "对比 'Boeing 727-200'" --> RAGImage[fa:fa-image <b>最佳 RAG 图像 (V_Ref)</b>]
    end

    %% 4. 工具执行 (核心)
    subgraph "STEP 4: 工具执行"
        direction TB
        ToolSelect(fa:fa-tools <b>工具选择</b><br>error_type: 'wrong_concept'<br>工具: RAG 辅助重生成)
        Dispatcher --> ToolSelect
        
        ToolSelect --> T2I2(fa:fa-magic T2I 生成 V2)
        RAGImage -- "V_Ref" --> T2I2
        
        T2I2 --> V2[fa:fa-image V2 图像<br>(已修正)]
    end

    %% 5. 循环
    V2 -- "<b>[ 循环: 检查 V2 ]</b>" --> VLM_Diagnosis
    RAGImage -- "<b>[ 更新记忆 ]</b><br>记录 V_Ref" --> Memory
    Success --> Stop(fa:fa-flag-checkered <b>结束</b>)

    end

    %% --- 样式定义 ---
    classDef default fill:#F9F9F9,stroke:#999,stroke-width:2px
    classDef io fill:#E8D4F5,stroke:#9673A6,stroke-width:2px
    classDef gen fill:#D5E8D4,stroke:#82B366,stroke-width:2px
    classDef core fill:#DAE8FC,stroke:#6C8EBF,stroke-width:3px
    classDef success fill:#C8E6C9,stroke:#4CAF50,stroke-width:2px
    classDef json fill:#FFF2CC,stroke:#D6B656,stroke-width:2px,font-family:monospace,font-size:12px

    class User,Prompt,V1,RAGImage,V2,Stop io
    class T2I1,T2I2 gen
    class VLM_Diagnosis,Check,Dispatcher,RAG_Flow,Stage1,Stage2,ToolSelect,Candidates,Filtered,Memory core
    class Success success
    class VLM_JSON json
```

![](../../../../pic/Pasted%20image%2020251031135945.png)
图3：顶部：我们方法的高级概述。给定文本提示<p>，我们使用文本到图像（T2I）模型生成初始图像。然后，我们为每个标题<cj>生成检索标题，从外部数据库检索图像，并将它们用作模型的参考以改善生成。底部：检索标题生成模块。我们使用VLM判断初始图像是否与给定提示匹配。如果不匹配，我们要求它列出缺失的概念，并为每个缺失的概念创建一个可以用于检索适当示例的标题。
