---
tags:
  - 计算机
  - 人工智能
  - 项目实战
  - ImageRAG实验数据
---

##### Omnigen
Method          | CLIP     | SigLIP   | DINOv1   | DINOv2   | DINOv3   | KID     
Baseline        | 0.2674   | 0.1101   | 0.6359   | 0.2987   | 0.2529   | 0.01266
BC_SR           | 0.2945   | 0.1392   | 0.7052   | 0.4664   | 0.4260   | 0.00566
BC_MGR          | 0.2986   | 0.1414   | 0.7131   | 0.4709   | 0.4260   | 0.00535
TAC_SR          | **0.3054**   | **0.1430**   | 0.7216   | 0.4766   | **0.4383**   | 0.00521
TAC_MGR         | 0.3046   | **0.1430**   | **0.7301**   | **0.4818**   | 0.4338   | **0.00414**


Method          | CLIP     | SigLIP   | DINOv1   | DINOv2   | DINOv3   | KID    
TAC_MGR         | 0.2984   | 0.1381   | 0.7150   | 0.4606   | 0.4265   | 0.00327

```python
            # [Scheme 3] Curriculum Learning
            # 判断是否比上一轮有显著进步 (Score 提升超过 2.0)
            is_improvement = (len(failed_attempts) > 0) and (score > last_score + 2.0)

            # 触发条件：达到满分(8.0)，或者及格且分类正确(6.0+Correct)，或者有显著进步(is_improvement)
            if score >= 8.0 or (score >= 6.0 and taxonomy_status == 'correct') or is_improvement:
                if score >= 8.0 or (score >= 6.0 and taxonomy_status == 'correct'):
                    f_log.write(f">> Success! (Score: {score}, Taxonomy: {taxonomy_status})\n")
                else:
                    f_log.write(f">> Partial Improvement! (Score: {score}). Attempting to learn...\n")

                # [GRPO Learning] 核心提取逻辑
                if len(failed_attempts) > 0:
                    # 获取上一次失败的尝试
                    last_fail_img, last_fail_prompt = failed_attempts[-1]
                    
                    # 调用 VLM 对比失败图和当前成功图，提取经验
                    new_experience = extract_experience_from_contrast(
                        class_name, last_fail_img, current_image,
                        last_fail_prompt, current_prompt,
                        client, model=args.llm_model,
                        reference_specs=reference_specs # [Fix] Pass Ground Truth Specs
                    )

                    if new_experience:
```